import React from 'react';
import { Box, Typography } from '@mui/material';

export function Footer() { // eslint-disable-line
  return (
    <Box style={{background: '#CCC', width: '100%', height: '100px'}}>
      <Typography>Placeholder</Typography>
    </Box>
  )
}  