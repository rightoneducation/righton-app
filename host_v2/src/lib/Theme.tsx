// Host's theme has been relocated to @righton/networking (DesignSystem/Theme.tsx)
// as part of the host/play UI refresh. This shim preserves the existing default
// import (`import Theme from './lib/Theme'`) used across host_v2.
import { HostTheme } from '@righton/networking';

export default HostTheme;
