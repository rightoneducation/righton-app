    export const handleChange = (value: string, index: number) => {
        
return { value, index };
      
    };

    const handleKeyDown = (e: React.KeyboardEvent, index: number) => {
       
    };

    const handleConfirmationError = () => {
   
    };

    const handleSubmit = async () => {
       
       
    
    };