import React from 'react';
import { styled } from '@mui/material';
import { ContentFrame } from './QuestionTabsStyledComponents';


export const CCSSContentFrame = styled(ContentFrame)(({theme}) => ({ // eslint-disable-line
  height: '280px'
}));