export enum ScreenType {
  GAMES,
  QUESTIONS,
  LIBRARY,
  SIGNUP,
  LOGIN,
  CONFIRMATION
}

export enum ScreenSize {
  SMALL,
  MEDIUM,
  LARGE,
}

export enum CardType {
  CORRECT,
  INCORRECT,
}
