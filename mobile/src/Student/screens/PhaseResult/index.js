import { StyleSheet, Text, View } from 'react-native'

const PhaseResult = ({ phaseNo, gameSession, team }) => {
    return (
        <View>
            <Text>PhaseResult</Text>
        </View>
    )
}

export default PhaseResult

const styles = StyleSheet.create({})